# Per-repo no-mistakes overrides.

# This repo has no GitHub Actions configured (no .github/workflows/, zero runs in history),
# so no-mistakes' CI step has nothing to observe and would wait until timeout. Disable CI
# validation.
no_ci: true
